---
description: "强制 AI 在使用任何技能（Skill）时明确声明技能名称和使用目的，提高工作流程的透明度和可预测性。适用于所有 Cursor 技能系统中的技能。"
alwaysApply: true
---

# 技能使用透明化规则

## 核心原则

**每次使用技能时，必须明确告知用户正在使用哪个技能以及为什么使用。**

这不是建议，是强制要求。透明度是建立信任和可预测性的基础。

## 声明格式

当调用任何技能时，必须在执行技能内容**之前**使用以下格式声明：

```
🔧 正在使用 [技能名称] 技能
目的：[一句话说明为什么使用这个技能]
```

### 格式要求

- **必须使用** 🔧 emoji 作为视觉标识
- **技能名称**使用原始英文名称（如 `my-skill-name`）
- **目的说明**必须简洁明了，一句话即可，说明这个技能解决什么问题
- 声明必须**独立成段**，不要与其他内容混在一起

## 示例

### 设计类技能

**用户说："帮我添加一个用户认证功能"**

```
🔧 正在使用 design-feature 技能
目的：在编写代码前，先探索需求并设计完整的认证方案

[然后开始执行技能的流程...]

让我先了解一下项目的当前状态...
```

### 调试类技能

**用户说："这个函数返回了错误的结果"**

```
🔧 正在使用 debug-issue 技能
目的：系统化地找出问题根因，避免盲目猜测修复

[然后开始执行技能的流程...]

## 阶段 1：根因调查
让我先收集信息...
```

### 开发类技能

**用户说："实现一个数据验证函数"**

```
🔧 正在使用 implement-with-tests 技能
目的：确保先编写测试，再实现功能代码

[然后开始执行技能流程...]

首先，让我编写测试用例...
```

### 工作流类技能

**用户说："帮我准备代码审查"**

```
🔧 正在使用 prepare-code-review 技能
目的：检查代码质量并准备审查清单

[然后执行技能流程...]

让我检查最近的更改...
```

### 文档类技能

**用户说："生成 API 文档"**

```
🔧 正在使用 generate-api-docs 技能
目的：自动从代码生成 API 文档

[然后执行技能流程...]

让我分析 API 接口...
```

## 适用范围

此规则适用于**所有技能**，包括但不限于：

### 1. 社区技能框架
- 任何来自开源社区的技能框架（如 Superpowers、awesome-agent-skills 等）
- 第三方技能库中的技能

### 2. 用户自定义技能
- 您自己创建的技能
- 团队共享的技能
- 项目特定的技能

### 3. 技能位置
- 全局技能：`~/.cursor/skills/`
- 项目技能：`项目/.cursor/skills/`
- Agent 技能：`.agents/skills/`

## 例外情况

**可选的例外：**
- 元技能（用于介绍技能系统本身的技能）可以不声明
- 非常简单的辅助技能（如格式化、简单转换）可以根据需要决定是否声明

**其他所有技能建议都声明。**

## 为什么需要这个规则

### 用户视角的好处

1. **透明度**：用户清楚知道 AI 在做什么，不会感到困惑
2. **可预测性**：用户知道接下来会发生什么，可以提前准备
3. **学习性**：用户可以了解每个技能的用途和触发场景
4. **可控性**：如果不需要某个技能，用户可以及时叫停

### AI 视角的好处

1. **自我提醒**：声明技能可以提醒自己要遵循技能的流程
2. **避免遗漏**：明确声明可以防止忘记使用应该使用的技能
3. **结构化**：声明技能有助于组织思路和执行步骤

## 执行清单

每次使用技能时，必须：

- [ ] 判断是否需要使用某个技能
- [ ] 使用规定格式声明技能名称和目的
- [ ] 声明独立成段，视觉上清晰可见
- [ ] 在声明后再开始执行技能的具体内容
- [ ] 完整遵循技能的流程和清单

## 反模式（禁止）

### ❌ 不声明就直接使用

```
让我先了解一下项目的当前状态...
[直接开始执行技能，但没有声明]
```

### ❌ 声明不清晰

```
我会使用一些技能来帮你...
[没有说明具体是哪个技能]
```

### ❌ 声明格式错误

```
现在我要用某个技能了
[格式不规范，没有使用指定格式]
```

### ❌ 声明和内容混在一起

```
让我使用某技能来探索需求，首先查看项目文件...
[声明和执行内容没有分开]
```

## 正确示例

### ✅ 清晰的声明

```
🔧 正在使用 design-feature 技能
目的：在编写代码前，先探索需求并设计完整方案

[空一行]

让我先了解一下项目的当前状态...
```

### ✅ 多个技能的串联

```
🔧 正在使用 design-feature 技能
目的：设计用户认证功能的完整方案

[执行设计流程...]

设计已完成并获得批准。

---

🔧 正在使用 create-implementation-plan 技能
目的：将设计分解为可执行的实施计划

[执行计划创建...]
```

### ✅ 不同类型技能的使用

```
🔧 正在使用 analyze-codebase 技能
目的：分析现有代码结构，找出需要重构的部分

[执行分析...]

分析完成，发现 3 个需要重构的模块。

---

🔧 正在使用 refactor-with-tests 技能
目的：安全地重构代码，确保测试覆盖

[执行重构...]
```

## 自定义格式

您可以根据需要自定义声明格式，但必须保持一致性：

### 可选的格式变体

**简洁版：**
```
🔧 [技能名称]: [目的]
```

**详细版：**
```
🔧 正在使用 [技能名称] 技能
目的：[目的说明]
预计步骤：[步骤数量]
```

**分类版：**
```
🔧 [类别] > [技能名称]
目的：[目的说明]
```

选择一种格式后，在整个项目中保持一致。

## 总结

**记住：透明度是信任的基础。每次使用技能，都要让用户知道你在做什么。**

这不是额外的负担，而是专业性的体现。无论您使用什么技能框架或自定义技能，这个规则都能帮助建立更好的人机协作体验。
